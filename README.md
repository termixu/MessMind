# MessMind
Decentralized neural network over mesh networks. No cloud. No server. No single point of failure.  MessMind is a swarm of devices that collaboratively train and run AI models without any central coordinator. Nodes communicate only through local mesh (Wi-Fi, LoRa, Ethernet) and exchange knowledge — never raw data.
